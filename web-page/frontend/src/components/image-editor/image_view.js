//import React, { useEffect } from 'react';

//TODO: Component for future view of image and layers.
function ImageView(image) {
    
    return(
        <div>
            <img src={image.path} alt="uploaded"/>
        </div>
    );
}

export default ImageView;